{
  "name": "sankey-sunburst",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "lint": "npx eslint src/ --max-warnings=0",
    "lint:fix": "npx eslint --fix src/ --max-warnings=0",
    "serve": "lite-server",
    "start": "npm run serve & npm run watch",
    "watch": "nodemon --ignore dist/ --exec npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "nodemon": "^2.0.19",
    "rollup": "^2.77.0"
  },
  "dependencies": {
    "eslint": "^8.20.0"
  }
}
